[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "y42_stepper"
version = "0.1.0"
description = "ZDT Y42 dual-firmware (X/Emm) closed-loop stepper driver"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.8"
